# Copy this file to .env and adjust as needed

# SQLite (default — no extra setup required)
DATABASE_URL=sqlite:///./prompts.db

# PostgreSQL example (uncomment and install psycopg2-binary to use)
# DATABASE_URL=postgresql://user:password@localhost:5432/prompt_ingestion
